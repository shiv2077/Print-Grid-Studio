# apps/web/.env.local — browser-exposed config ONLY. Copy to apps/web/.env.local (gitignored).
# NEXT_PUBLIC_* values ship to the client. NEVER put secrets here (no key_secret, no service_role).

NEXT_PUBLIC_RAZORPAY_KEY_ID="rzp_test_..."
NEXT_PUBLIC_API_URL="http://localhost:3001"
